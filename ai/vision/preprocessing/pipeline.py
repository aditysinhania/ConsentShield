"""Vision preprocessing helpers (resize, normalize) — placeholders."""

from __future__ import annotations

from typing import Any


def preprocess_screenshot(image: Any) -> Any:
    """Normalize screenshot for detector input. Identity stub for now."""
    return image
